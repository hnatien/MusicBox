services:
  music-box:
    container_name: music-box
    build:
      context: .
      dockerfile: Dockerfile
    image: music-box:prod
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped